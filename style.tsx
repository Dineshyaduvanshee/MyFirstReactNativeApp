import { StyleSheet } from "react-native";

 const ExStyles = StyleSheet.create({
    textBox: {
        color: '#fff',
        fontSize: 35,
        backgroundColor: 'red'
    }
});
export default ExStyles;